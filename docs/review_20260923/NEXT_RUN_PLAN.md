# 下一轮执行计划（以审查提交9e8bee77为依据）

本文件是建议的有界任务，不表示已启动训练。保留原A/B、历史数字链、有效checkpoint和结果；不要再搬目录、另建发布工作树或全量重训。

## 0. 先核对是否已有更新

逐项比较当前HEAD与9e8bee77。已经修好则运行对应回归，不覆盖新代码。运行中的任务不得热改其依赖。

## 1. 只修本次确认的局部问题

1. `predictor_innovation.py`：所有selected记录统一携带`arm_key`，尾部按记录与SHA加载；用现有合法选中4000步checkpoint补评测，先不重训。记录当前执行源码身份，区分旧completion。
2. 预测创新的配置/接口一致：当前TX实际只读[residual,Ptx]。选择保留并按真实pipeline解释，或另建匹配实验让两臂都读Fb_TX/Ptx。不要改完接口直接复用旧指标。
3. `linear_measurement.py`：norm控制包失败时对完整delta屏蔽/回退基础；W拟合放入每个seed循环；completion只在全覆盖后发布；资源仍为32控制+992测量=1024，不免费发送norm。
4. 配对统计左右两侧都用同一完整键检查器，拒绝重复或不匹配的seed/preprocessing，不只检查各有3行。
5. 计时v3补神经三臂的波形、实际Fb_RX、输出一致性检查，移除纯数字CPU-GPU-CPU冗余搬运。不要为计时重新训练。

验收先使用本报告对应反例，再少量真实权重。只重统计则不重推理；线性W/回退变了只重线性；新预测器接口另登记匹配训练。

## 2. 原1024模型做一个诊断矩阵

固定checkpoint和已训练SNR条件，交叉比较：
- 实际Fb_RX / 无误码Fb_TX；
- 实际增强AWGN / 增强噪声置零。

保持源图与生成规则不变，所有实际失败仍在正式结果中；特权输入只能用于诊断。再列Dc(F)连续表示参考。输出PSNR/LPIPS、normalized-latent误差，区分压缩、信道噪声和基础不一致，不将dB差说成物理误差占比。

## 3. 第一条新训练：同F、同Dc、全部N4084用于纯连续传输

目的不是故意找一个弱对照，而是确认混合链的数字基础是否值得占用3060 uses。

- 复用原视觉Encoder与阶段A Dc，不用VAR补全和类别信息；
- 输出恰好8168实坐标（4084复符号），归一化实际发送能量E8168；
- 不得直接截断未经相应训练的4096-use模型；
- 复用20k/1k数据，原loss和合理训练机会；
- 与raw/算术数字自适应、原1024增强及适配版同SNR/同source比较；
- 新方法最终报告质量与完整TX/RX成本，不把校准和development混用。

这一项先只做一个预算，不平铺所有长度或重训全部外部模型。

## 4. 现有分配点收口为系统策略

固定N4084，已有三种m8分配和数字m7/8/9候选。仅在calibration冻结选择，完整费用/失败均计入；所有混合端点输出身份需对齐。

不要把简单查表重新命名成首次资源分配算法。其作用是准确定位系统适用区间。需要m7/m9混合时，应有相应表示与训练，不拿未适配的m8模型代表它们。

## 5. 只增加一条方法候选：轻发送端、接收端先验辅助的增强

TX不要运行完整VAR补全；使用F与真实前缀累计latent F_prefix，经小型增强Encoder输出1024复符号。前缀累计必须保持官方10尺度量化映射，只累计r1:m，不修改tokenizer的尺度定义。

RX仍用实际收到前缀完成VAR补全，再读取增强观测修正latent，最后Dc解码。原数字m8基础保持3060/E6120，额外1024/E2048。

与当前TX完整VAR残差编码配对训练/合理暖启动，同资源同Dc比较。目标是保持有用质量取舍同时减少TX prior推理，不预设一定减少多少毫秒。若质量明显损失，保留原方案，不强行保住轻量故事。

## 可低成本并行的参照

修复后的线性测量仅增加随机正交投影 vs 训练残差二阶矩/PCA投影两个固定基比较；相同1984测量维、范数费用、W拟合和Decoder。均值/中心化规则明确，不能只报解释方差。

分组功率留到误差诊断之后；coset、扩散、视觉骨干迁移不在本轮启动。

## 交付

报告一：本次局部修复、反例回归与受影响输出；
报告二：原1024误差诊断、纯连续控制、校准冻结的资源策略，以及轻TX候选（若已执行）的真实结果。

表格由同一份逐源记录生成，包含模式、N/E、Decoder、source/seed范围、模型与源码身份。独立holdout暂不打开；机制和选择规则收口后再安排一次正式验证。
