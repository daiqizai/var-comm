# Deferred actual N4084 replay

The historical audit established source/selected/policy provenance but left current complete metric/PHY/timing compatibility pending. A separate serial worker now implements the necessary real replay. It waits for actual C completion, main delivery-chain exit, archived-reference common-metric completion and that worker's verified exit, then for GPU0 to be cool and unused. It does not launch competing training or change the active chain's bound code.

The scope is six existing methods: selected full-TX control, selected light-TX, original1024 Dc, frozen raw m789, frozen arithmetic m789 and frozen resource lookup. Historical P4084 is excluded because continuous_grid_v1 already queues its actual selected shared-engine evaluation. No new method, retraining, policy selection or holdout is introduced. Existing weights and policies are checked against original hashes.

The adapter calls existing research and clean timing execution engines. The folded512 lookup endpoint has a small adapter that preserves its actual3572+512 uses, measures waveform energy, adds noise once to each disjoint segment outside RX timing, and derives RX base latent from the received base PHY. It does not pass the TX latent to RX. Quality and online timing invoke the same adapter; online VAE/token/VAR/arithmetic work is included. The existing transmitted class index remains the same paid side-information protocol, not a new classifier claim.

Before any bulk replay,24 real-weight checks cover original development indices0 and11, SNR1/13 and all six methods. They compare against historical metrics; the folded endpoint also compares pixels with its original engine. Any mismatch preserves a failure receipt and stops for review. CPU tests do not satisfy this gate. On success, the worker executes9000 real frames and600 uniform timing repeats, with three warmups for each of ten fixed source/method cells. Immutable per-source/method seals support safe thermal interruption and resumption. All failures, old/new metric differences and original history are retained; large images/weights stay local.

The output is `outputs/TOKEN-CHANNEL-EFFICIENCY-20260923/n4084_common_replay_v1/`. Inspect worker registration, actual PID/start_ticks/cmdline, worker_status, qualification and completion. The worker uses its own lock and existing thermal-aware Runner with a unique stage key. It never resumes the retired controller. CPU release/adapter tests cover frozen choices, actual waveform N/E rejection, and required upstream completion/process exits. Actual GPU acceptance and bulk results remain NOT_RUN until their receipts exist. Final comparison/publication and external-reference PHY/timing compatibility remain separate delivery requirements.

Single entry: `bash tools/run_legacy_n4084_replay.sh`, launched in an independent session only after checking the existing worker identity. Actual selected full/light checkpoints passed strict CPU loading with no missing/unexpected keys; this is not GPU quality acceptance.
