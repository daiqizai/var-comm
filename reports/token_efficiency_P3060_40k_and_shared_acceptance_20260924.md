# P3060 calibration stop and shared execution acceptance (2026-09-24)

P3060 completed 40,000 updates and the existing scheduler stopped further extensions. The last two full-calibration utility changes were +0.080443% and -0.470516%; the registered rule requires both intervals to improve by at least 0.2%. The final interval worsened and is retained. The full-history selected checkpoint is step 37,500, utility 0.02066353092204469, SHA256 `962a1de0b7f25ba080c04b6e2b91b3c1cb999878985509ebbb917f4b55cce794`. This is a calibration-rule stop, not proof of convergence. P2048 remains finalized at 30,000.

The existing CPU publisher independently verified all 17 rounds / 255,000 calibration records, including complete unique 1000-source x five-SNR x three-noise grids, source identity, finite metrics, hashes, full-history selection minimum, actual parent and selected checkpoint hashes, original and extension execution identities, decision and finalization. This publication adds only the four new rounds (60,000 records) and preserves SHA references to earlier rounds. The curve derives from the same verified rows. Cumulative recorded training/calibration times are 16,025.430s / 3,096.457s, not wall-clock forecasts. Microbatch 4 / effective batch 16 and the original precision, loss, models and protocol remain unchanged.

The real shared TX/channel/RX acceptance then completed on GPU0 using original calibration sources and actual frozen VAE/VAR/Dc weights. Its 32 entries comprise eight continuous interface checks, 20 encodable digital checks and four explicitly unencodable raw length constraints. Digital checks cover m6/m10 endpoints, N2048/3060/4084, raw/arithmetic and QPSK/16QAM where encodable; actual noiseless and 1dB received reconstructions replay exactly. Prepared/offline and online TX waveforms match. Low-SNR body failures remain in the receipt; they are not acceptance failures when the registered receiver behavior replays correctly. Actual 16QAM energy varies and is retained separately.

Qualification scope matters: the continuous communication heads in this small interface probe were disposable seeded models, not the selected trained P2048/P3060 weights. Selected-model replay qualification is still required at their later evaluation. Frozen model states and absence of gradients were checked; the receipt generates no formal population quality metrics. A separate CPU audit verified all 56 registration bindings, the acceptance registration SHA, successful scheduler exit and exact completion snapshot SHA.

The original detached scheduler has entered QPSK full calibration. Policies will freeze from calibration before development. No new queue, holdout, content selector or hardware setting was introduced, and no active bound source was edited. Historical metric/parity/timing workers continue waiting for their registered predecessors. Full digital/continuous development, B2, C, historical GPU checks and final paired publication remain pending.

Artifacts: [P3060 40k calibration, curve, selected and stop lineage](../results/token_channel_efficiency_20260923/budget_extensions/P3060_seed2026092304_until40000/); [shared real execution acceptance and binding audit](../results/token_channel_efficiency_20260923/execution_qualification_v1/).
