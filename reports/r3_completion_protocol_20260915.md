# 冻结R3收尾协议

2026-09-15，本机UTC+8。使用已选定的10000 checkpoint（SHA `667abc43639c09b0f0c465bc3bf669117ff727095ffdb7e563f6dad71c11ab3e`），不改权重、不重选或续训。

质量：原100个development源，2001/2002/2003噪声，原1/4/5/6/7/13/19 dB。主1/4/7、高13/19及5/6过渡点分开。复用22个已封存参考，仅新增2100个R3传输结果；由已有CPU分析驱动核对归档、冻结参考、原校准选模和源图级配对统计。

时间：只新增R3一个方案，沿用同端点实验的32个源、五SNR、seed2001和每条件三次计时，共480行。CPU uint8裁剪RGB到CPU波形、CPU原接收波形到CPU RGB，包含必要归一化、编码/量化/表示转换、通信E/R、图像Decoder及搬运；不含文件IO、加载、预热、离线验证、空口或排队。每SNR预热两次，batch1、FP32、TF32关闭，GPU0无外来计算进程时执行。

逐条TX波形和RX图像对R3本身的已完成质量归档核对，固定容差1e-6并报告实际逐字节一致率，观测字节保持不变。起止模型SHA相同。继承旧七系统3360条计时记录，不执行原R3目录的16模型计时或finisher。

时间比较按源图先平均重复与所选SNR，再做10000次paired bootstrap，seed9142026。**这里是同源图、同CPU端点、不同session的补测**；新R3只有WeTok/R3模型驻留，旧七系统共同驻留，硬件温度、CPU负载等也非逐调用匹配，不将小时间差称为纯向量机制导致的速度收益。此限制不影响原配对质量比较。

没有新正式test，也不以本阶段决定数字模式阈值。该项完成后继续整帧熵编码和校准模式对照，不启动新学习分支。
