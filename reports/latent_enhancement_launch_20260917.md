# m8数字基础＋额外连续latent增强：启动记录

本机时间2026-09-17 01:58（Asia/Shanghai，对应UTC2026-09-16）。本记录是工程启动状态，不是质量结果。

## 当前真实状态

GPU0（RTX4090D，24GiB）开始时无计算进程，仅96MiB显示占用。用户确认可用后，只启动本新实验，不恢复HiFi或已结束的混合/WeTok训练。启动快照流水线PID466481，缓存worker466513；以实时状态文件和进程命令为准，禁止根据过期PID重启或停止任务。

`outputs/VAR-LATENT-ENHANCEMENT-20260917/pipeline_001/status.json`是总入口；缓存`cache_v1/status.json`，训练`stage_A_v1/status.json`。截至本快照1000/1000校准和300/20000训练源的精确latent已保存。缓存运行利用率约82%，显存约3.1GiB；此阶段VAR单样本补全是为了保持原硬选择规则，不以显存填满为目标。

一次早先非持久shell后台启动未存活、未留下缓存或模型更新；之后已用常驻受监控session重新启动并确认实际GPU、日志和分片进展。没有将该次启动声明当作训练进度。

## 已完成的真实自检

`qualification_001/completion.json`及8项CPU测试记录以下事实：

| 项目 | 结果 |
|---|---|
| 官方encoder+quant_conv实际F形状 | 32×16×16 |
| 全部十尺度真实token累积与官方Fq映射 | 最大差0 |
| 1dB、body CRC失败时的原基础输出回放 | RGB最大差0 |
| 19dB成功时的原基础输出回放 | RGB最大差0 |
| 追加增强后的前3060发送/接收坐标 | 与原数组逐项相同 |
| 512/1024增强层 | 独立能量归一化；独立噪声且512嵌套于1024流 |
| Dc与D0 | 参数存储不共享；一次Dc更新后原VAE状态SHA未变 |
| Dc参数冻结后的输入梯度 | 非零，L1约0.8591 |
| Dc训练参数 | 64,663,171，仅复制post_quant_conv+decoder |
| 四样本资格更新耗时 | 0.409秒，仅单次工程测量，不作全训练ETA |
| 资格检查峰值allocated/reserved | 16,018,290,688 / 20,036,190,208 bytes |

资格更新只使用训练图，全部丢弃；正式训练从官方D0的独立副本与fresh AdamW开始。保留原64.7M Decoder副本是用户指定的Decoder适配，不是新增通信大网络。后续增强通信网络仍限制为轻量卷积结构。

## 正式排程

1. 从原20k/1k图像建立FP32 F/Fq/Fb_TX，核对原数据manifest和源图ID；不把旧FP16 fhat当精确基础。原数据/权重只读、不复制大数据集。
2. 缓存后自动启动阶段A：逻辑batch16、microbatch4，50%F/25%Fq/25%独立alpha插值；MSE[0,1]+0.1LPIPS[-1,1]。
3. 每500步固定100源校准，每2000步完整1000源；选模固定50/25/25混合图像效用，另列各分支PSNR/LPIPS及原D0参考。DINO不训练、不选模。
4. 最少4000更新、40k仅作安全保护；三次完整校准所有相关曲线停滞才按规则判停。尚未测得完整逻辑更新或完整校准速度，不给虚假的完成时间。
5. 阶段A结束写`continuous_reference_result.json`：若Dc(F)未显示可利用改善，则停止机械启动两个码率并报告问题；若合格，再进入已授权的增强E/D及receiver-only控制。目前阶段B尚未启动，不能称整个实验完成。

`scripts/run_stage_a.sh`支持从完整缓存与模型/Adam/数据顺序/RNG恢复。遇其他授权GPU任务时在安全边界保存并释放显存；只自动重试资源让卡，不无限重试代码错误或显式中断。历史checkpoint、失败和中断记录保留。

## 尚未完成，不预先下结论

Decoder适配质量、512/1024新增信息增量、与同N3572/4084的raw/实际整帧算术数字及同Dc竞争结果都尚未产生。原m8+D0、m9/N3060、同Dc无新增传输精化器及R3/Deep/外部自然预算参考均保留在计划中。最终在线TX必须计入发送端Fb_TX生成，缓存不免除该成本。旧固定m7/RGB混合负结果不改写。
