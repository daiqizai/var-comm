# Single-worktree migration, 2026-09-23

The sole working repository is `/home/liulu/projects/VAR_COMM`. The root and ancestors had no Git repository; the clean old `publish/var-comm` repository was based on `1f34e68b3ca4884f449119ff1622875b354257d2`. A new root Git metadata directory was initialized and attached to that existing history using fetch, update-ref and index-only read-tree. No unrelated history, force push, reset, asset move, or visibility change was used.

Both source trees, all old Git refs in a verified bundle, byte hashes, effective diff and per-path merge decisions were saved under `_migration_backup/20260923_003731/`. The old publication tree remains intact and ignored. All 307 publish-only tracked paths were restored once. Root training repairs in entropy, hybrid_training, mode_policies and whole_entropy were retained after content comparison; timestamps were not the merge criterion. No own training process was running when source paths were updated.

Six self-written dependencies formerly loaded from a sibling project are now present with exact byte provenance. Runtime imports use root source; third-party model assets retain their existing paths. Three publication-copy entry points now reject that retired workflow. Both previous README/AGENTS versions and the old release manifest are preserved under docs/history.

Tracked scope includes actual source, configuration, scripts, tools, tests, reports and lightweight historical results. Twenty-one result files were copied without alteration from existing output runs, with run IDs, original paths and SHA256 in result_lineage.json. The source_export_003 coverage receipt accounts for all 113 entries; operational _codex_sync backup receipts remain excluded. Datasets, weights, tensor caches, environments, build products, secrets, outputs and the old nested publication repository are excluded. Public upstream package metadata is not project source and is excluded.

CPU verification runs with a replaced PYTHONPATH and checks imported self-written modules against the checkout root. Existing third-party Python packages may be supplied by the environment. Historical calibration replay now explicitly isolates each budget/protocol/archive; missing historical decoder SHA is labeled UNRECORDED rather than invented. Frozen selected actions must still reproduce exactly. The root manifest records actual modified source and preserves original provenance instead of asserting unchanged source.

This migration does not certify previous repair completion, rerun image metrics, or establish historical decoder identity. GPU/real-weight quality acceptance: NOT_RUN (outside this migration scope). No original A/B retraining and no new methods. CPU and remote-checkout receipts are delivered separately in results/repository_migration_20260923/.

Final ignored-source audit found three existing _codex_sync changes not yet in the worktree: rejection of duplicate full pairing keys and two stronger regression files. These were reviewed and integrated once; other sync model/predictor source matched root. Runtime snapshot sources in outputs remain historical evidence, not import targets.
