# 20源探索阶段的GPU资源中断说明

2026-09-17 00:46:27，原作者驱动在下一帧开始前检测到外部GPU进程367282（观察到的路径为CAP-VPR环境），按既有资源保护规则退出。这不是采样/译码失败，也不是模型性能负结果。

- 已提交91/100帧全部保留：48复用原合法结果、43新计算。中断在新一帧开始前，不把该次资源停止当成失败图像或删除已完成记录。
- 没有向CAP-VPR进程发信号，也没有改变其环境/参数。复查时该进程已退出。
- 原实现、checkpoint、FP32、sampling23、noise2001、完整步数、元数据/失败规则及20源名单均不变。
- `scripts/resume_stage20_when_idle.py`在GPU持续空闲后以原版本继续。新监视器只在发现其他GPU进程时中断我们自己的worker并等待，不修改其他任务，不自动换采样或模型。

## 对计时的影响必须单列

外部进程的精确GPU开始时间未被原逐帧边界日志记录。保守标注停止前最后两条已完成记录：19dB源47和52；原完整RX分别约94.217秒、74.950秒。不能仅凭某次变慢就断言其原因或声称整个帧都在共享GPU。

这两条**质量结果仍保留**，原始耗时也不改写；不能把被标注的原始时延无条件当作独占GPU性能。统一成本图的预登记共同10源不含47/52，所以该已知窗口不进入该图的时间均值；HiFi全100帧原始耗时分布则须同时披露此标记。

原240帧/1500计划保持原位，当前是不同范围的100帧探索队列。资源中断日志与恢复日志位于新阶段`gpu_contention_note.json`、`resource_resume_001/`，不是原1500完成回执。
