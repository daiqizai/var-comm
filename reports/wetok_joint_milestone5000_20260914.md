# Joint E/R 5000更新：全校准与选模

2026-09-14 15:49完成三臂各5000次新增更新，15:50完整校准及CPU审计PASS。模型、Adam、配对数据/增强/SNR/标准噪声、N/E及选模均通过既有检查。里程碑SHA：`b218c5447cd1fbb317454254921acd737a76ae2fe088191f164449b186868694`。

## 三个基础结构的同机会校准

1000张完整校准图，1/4/7/13/19 dB和固定噪声。两侧各有5000更新机会，均只从完整校准点按LPIPS选择。此次三Joint及对应三R-only均选择5000，不再选择起点；1000/2500阶段曾选择0的事实仍保留在旧回执和曲线中。

| 接收结构 | Joint PSNR | Joint LPIPS | R-only PSNR | R-only LPIPS | Joint−R-only LPIPS |
|---|---:|---:|---:|---:|---:|
| single-pass | 21.31552 | 0.197125 | 21.25567 | 0.198284 | −0.001159 |
| multiscale no-history | 21.25335 | 0.197853 | 21.24295 | 0.199511 | −0.001658 |
| multiscale state-history | 21.31679 | 0.194679 | 21.27676 | 0.199027 | −0.004347 |

Joint内部state−single LPIPS为−0.002446，state−no-history为−0.003174。与2500时不同，条件版在5000完整校准上已优于两个基础对照；这提示不能用3500监控子集的一次反弹判断训练终局。

**这仍是选模人口，不是独立确认。** 不能据该表宣布最终通信机制成立。尤其是R-only完整网格创新量强参照同5000机会的校准LPIPS为**0.187311**、PSNR为21.57362，仍优于Joint state。三个基础结构的图不是全部方法排名，不能漏掉这个强对照。

诊断bit错误率也不与图像结果等价：Joint state为0.115941，匹配R-only state为0.114368，前者bit错误率略高但校准LPIPS更好。它们均是连续接口的硬符号诊断，不是CRC可靠恢复率。

## 接下来的实际流程

已自动启动原注册development回放，首次实际保存1张源图的336行结果；evaluator PID为443167，finisher为348503。不要重启已正常结束的trainer348501/reviewer348502，也不要手动再开一份评测。

完整范围仍为三Joint＋六R-only＋七系统参考，33600主行、600 Deep支持点、900无噪诊断，之后由原脚本做图像级配对统计及CPU档案复核。原100张仍是development，不访问新holdout，不改模型/选模规则，也不根据中途结果删行。

只有完整回放后才判断三项：Joint相对匹配R-only的变化、Joint条件相对single/no-history的独立增量、相对全网格/数字/Deep强系统的残余差距。若条件结构出现增量，还需匹配Joint普通全网格迭代对照，不能把三阶段计算本身叫next-scale优势。

全部基础结构最优点位于本次末端，并不证明已经充分收敛；5000不是总研发上限。是否继续更多更新或先补结构控制，要结合完整回放及已有曲线，不仅凭“最优在末尾”保证更多训练必胜。

## 成本与曲线

累计已观察训练/校准墙钟GPU时段约2.93481小时。运行期间曾出现其它GPU进程，故该数字不是独占算力费用测量；被动观察器357923继续覆盖评测，最终时延要附并发背景。

2500与5000的完整校准、固定100图监控和训练分项曲线已单独生成。完整校准图保留step0并标注实际选中点，监控不参与选模；训练只用尾随100更新均值，不混成校准性能。入口`reports/wetok_joint_calibration_figures_20260914.md`。没有新模型推理或改变任何原始分数。

原始数据：`outputs/WETOK-JOINT-SENDER-R1-ANALYSIS/calibration_0005000/`；实际后续状态以`experiments/wetok-joint-sender-r1/docs/CONTINUE.md`及进程为准。
