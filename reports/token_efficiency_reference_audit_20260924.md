# Historical reference reuse: actual audit and queued common metrics

The audit checked all100 original development source pixels against the current preprocessing, complete five-SNR/three-noise keys, paid totalN/E, and15000 real received images across9300 archives. Ten preserved reference workpoints passed. Pixel hashes are unchanged; maximum PSNR recomputation difference is0.000005082dB. No old artifact was modified; no GPU, model forward or new holdout was used.

The four N3060 references are historical raw/adaptive arithmetic digital, perceptual DeepJSCC and WeTok R3. Three ADJSCC and three paid-metadata Swin points retain their natural resource budgets. None is relabeled as a common-Dc causal ablation. Different nominal-noise namespaces/lengths do not become identical observations merely by sharing seed indices.

The new independent rescore worker waits until the registered B1/B2/C chain has completed and exited, then recomputes the same frozen common quality metrics on the verified received RGB. This adds no model training, channel samples, mode selection or new test sources. It uses per-source immutable cells, actual GPU thermal-safe pauses, complete attempt retention, and before/after frozen metric hashes. Unknown failures stop. Four new CPU tests verify full-key coverage, source identity, resource accounting, nonfinite rejection and archive path confinement; these are engineering tests, not GPU metric acceptance.

Common GPU metrics remain NOT_RUN until the actual completion receipt. Historical N4084 comparison lineage and complete timing-context audit remain pending; this receipt does not close the entire merged study. No learned selector or new holdout is launched.
