# 外部定位：先完成非扩散部分的执行登记

日期：2026-09-16。依据用户“你先把别的搞完”的优先级调整；不改变方法、训练、数据或HiFi采样协议。

## 已完成和本次先做

- SwinJSCC三个自然码率、两种信息协议：4500次实际传输、9000协议视图；ADJSCC三公开权重：4500次。指标已经完成。
- 原四个N3060强系统的6000行原输出只补了同口径SSIM，不重跑网络。
- 先在CPU完成已完成工作点的逐SNR/主区间/高SNR表、源图配对bootstrap、在线成本整理、预登记图片0/25/50/75和seed2001的原尺寸PNG与拼图。
- HiFi未满1500帧前不进入排名，不以当前首批低SNR样本代表全体，不减少采样步数或改变生成seed来加速。

## 两个必要的共同预算数字点

沿用已登记的N4204/E8408、N4498/E8996，只有raw/整帧算术两族、m7/m8/m9和原FEC。配置：`experiments/external-baseline-positioning-20260916/configs/digital_common_budget.json`。

- 校准：原1000中`rint(linspace(0,999,100))`的100源，4101/4102/4103，1/4/7/13/19dB；两个预算共18000次候选传输。
- 规则不变：原BLER<=0.10的可靠性规则作为比较；最终图像LPIPS最小，PSNR不得比同族可靠性规则低超过0.25dB。DINO只报告。校准完成后冻结每预算/族/SNR的模式。
- 开发：只运行冻结的两族动作，原100源、三个原噪声、五个SNR、两个预算，共6000次。不读取新holdout。
- PHY全部真实重编码、rate matching和译码，不把旧N3060的质量移植成新预算结果。原算术bitstream允许复用，因为视觉/概率/源token都没有变化。
- 图像仅允许对完全相同的实际接收参数作函数memoization。原干净算术缓存先由原波形和原噪声实际重译，按收到的类别、mode、length和payload逐字节匹配；不得按真值修复错误候选。所有新候选和失败仍进入质量。
- 计时不使用任何TX/RX图像缓存。CPU像素到完整CPU波形，CPU接收观测到CPU RGB，FP32/batch1、GPU0、32固定源/seed2001/三次重复；仅补新增数字预算，不重跑旧整套benchmark。

## GPU排程与恢复

CPU表格和PHY可在HiFi运行期间完成。短GPU收尾如需优先执行，只暂停本任务自己的HiFi队列：记录原PID/完成帧/输入SHA，保留全部已提交帧和中断痕迹，再运行数字渲染/必要计时；之后用原冻结队列和完全相同输入续跑。正在执行的一帧若未提交，仅重做该帧，不能漏掉任何传输。不得停止其他用户任务或改共享环境。中断是用户优先级调度，不是方法失效。

最终非扩散报告清楚标出HiFi尚待；完整外部定位报告仍需HiFi同观测对比完成，不把这次阶段性交付当全部任务完成。
