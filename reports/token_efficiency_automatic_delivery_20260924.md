# Automatic authorized experiment continuation

The existing P2048/P3060 supervisor remains the only current GPU owner. A detached downstream chain waits for both20k receipts and supervisor exit, then applies calibration-only extensions, real shared-path acceptance, complete QPSK/16QAM calibration/development and online timing, B1/B2 source statistics, and merged short-prefix C.

C includes matched-pair continuation, N3060 V/P with shared P3060, two additional training seeds for the selected short-prefix candidate and direct controls, selected-model evaluation/timing, received-base diagnostics, calibration cross-noise diagnostics, paired source intervals and separate training-seed variation. No new holdout or learned selector; original A/B is not retrained.

New CPU module tests:67 passed (synthetic engineering, not quality). The historical selected P4084 checkpoint was loaded on CPU and its actual SHA verified: b6bd1f0bb01bbd734eeb6a63afac1b8f7c57998085c616011f37554035434e1a, selected step10000. GPU acceptance for the newly queued stages is NOT_RUN until their real stage receipts exist. The first whole-repository test run had156 passing tests and one expected stale release-manifest failure; after updating that manifest, all157 repository tests passed. Repository and release validators also passed.

Signal-aware cache pauses wrap the original unchanged cache algorithm; no tensor math or old registration is replaced. Immutable cell seals reject wrong identity/tampering and preserve interrupted data. Unknown failures fail closed, owned thermal pauses resume after cooldown, every signal checks process identity.

Outputs are server-local until actual results are reviewed. Historical external-reference compatibility and required reevaluation remain an explicit delivery item, as do exact remote publication/independent checkout. A completed grid is not advertised as the entire study complete.
