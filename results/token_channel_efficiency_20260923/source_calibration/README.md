# Completed real source calibration

All1000 registered calibration sources completed actual m6--m10 raw and integer
arithmetic bitstream generation, token-exact independent autoregressive roundtrip,
and20 D0/Dc representation reference paths per source.25000 per-source rows and
10000 ledger rows are preserved as plain CSVs with original
and published hashes in lineage.json. No model weights, pixels, tokens, bitstreams
or tensor caches are included. Summaries keep calibration separate from development.

The three targets in the experiment's quality_targets.json were recomputed and
verified from this CSV before publication. They were frozen by the source runner
before its dependent development stage started. All targets remain in scope,
including unreachable outcomes. They are calibrated display criteria, not evidence
that a communication system meets them. These source-only references have no
finite-channel N or E and must not enter a communication ranking.

The full original100-source development A stage and B1/B2 communication measurements
have separate live completion records. This calibration receipt does not certify
those later stages. The actual source implementations are bound by SHA in lineage.
