# 用户反馈后的主线边界

本机UTC+8日志2026-09-15：用户追问是否尚未找到主线。已明确承认：研究方向明确，但尚没有能由强对照和独立验证支撑的next-scale通信主方法；近期通用接收器归因/评测工作不能替代方法主线。

R3按已登记的10000训练机会、完整质量和独立时延收尾，作为有限的辅助归因。不要因R3某个点好看而自动再开通用接收器小变体、更多重编码/门控/归一化消融，更不能把普通full-grid迭代改名next-scale。

R3结束后的下一项主任务是形成**单一、具体、可证伪的通信问题与系统结构**：尺度条件结构如何改善同资源下源图信息的传递或恢复，为什么不能由普通重复计算解释，主要强对照是什么，N/E/CSI与可见观测如何固定。参考已有层级JSCC/逐次精化原论文和实现，不能把层级表示或神经网络本身当贡献。

可以保留或改进WeTok作为视觉工具，原VAR数字自适应/Deep与当前同WeTok基线保留。是否设计发送侧信息组织、分级观测或其它尺度条件机制，要由明确的问题和证据选择，不预先要求某个模块必胜，也不同时改多个因素。

这不取消既定R3收尾，不中断其他任务，也不停止通信研究。当前仍没有新holdout或训练重复证据，整个研究目标不能标记完成。
