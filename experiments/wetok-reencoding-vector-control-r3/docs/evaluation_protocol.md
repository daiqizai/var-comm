# R3最终比较登记（2026-09-15，本机UTC+8）

本文件在尚未查看R3首1000完整校准结果、且未进行任何新development推理时登记；训练仍执行原`protocol.md`，没有改变已登记的主区间或选模规则。当前只登记最终矩阵，评测驱动尚待实现，不把本文件当作已评测结果。

## 人口与完整历史

原100张development、七SNR、三噪声；只新增R3 prediction-vector，保留R2全部22方法，合计23方法48300主行，其中2100新推理。1600无噪名义19诊断行、600固定支持Deep行完整保留。原图像、分数、模型选择、源像素与物理字段不能随复用而改变。

主要比较`r3__full_grid_prediction_features`−`r2__full_grid_innovation`：原7000父点、相同参数/零融合初始函数、fresh Adam0、完整10000机会、相同数据/损失/噪声。两者Joint E/R训练后可有不同y，不声称固定同一发送器的纯接收消融。R3自身完整校准最优点可能不是10000，必须披露实际点，不强制末端。

R3对R2其它三个结构属于同10000机会、但未必等计算/参数的结构参照；对更早18个方法是历史或系统级参照，不冒充相同训练历史。保留同WeTok数字、VAR固定m8/自适应以及感知Deep，不能只选择有利对照。主区间仍是1/4/7 dB，5/6 dB使用固定支持点Deep补充，不用离支持点异常制造优势。

## 质量、统计、独立时间

全部新方法对旧22方法共22组差；八个SNR范围、六个指标，1056条配对区间。先对每张源图平均SNR/噪声，再用原10000次source-image bootstrap，原seed不改。支持点16学习模型对Deep共288区间，其中R3新增18，其余为不变数据的旧参照，不当新结果重复计数。

质量阶段不产生当前时延，显式共享时沿用8 GiB分配器上限、8 GiB启动/2 GiB源间空闲检查；不保证整进程或算力隔离，不改MPS/驱动/时钟。

独立时间固定原32源×五SNR×seed2001，新R3加旧15学习模型全部重新同场计时，共2560行。轮换16方法顺序，回放每个模型质量阶段的实际s/y与图像，不把旧时延混入新表。R3对15学习参照、七SNR范围共105区间。主要速度对照也是R2 residual；旧数字/Deep历史时间不混排。发现其它GPU进程则保留已提交源图并等待，不停止其它任务。

所有产物在本实验新outputs下，旧R2/更早结果只读；CPU图像/波形/特征审计、全矩阵和选择核验需完成后才发布结论。新正式holdout不访问，DINO不进入训练/选择，不把残差能量或BER降低单独当系统成功。
