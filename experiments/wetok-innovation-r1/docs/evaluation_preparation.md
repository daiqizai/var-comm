# 接收实验评测准备（本机UTC+8 2026-09-13）

训练保持原样：六臂同一冻结发送器、原损失与数据/噪声、首1000接收更新。新代码不修改训练/profile绑定源码或校验值，不新增GPU训练任务。

## 已实现与检查

- 新入口`evaluate.py`、`analyze.py`：读取已审计完整校准选模，13方法/27300主行，另600个Deep固定支持点，1968主配对与108支持点区间。
- 新六臂逐帧同s/y，保存真实信号/观测及其hash；旧三geometry、数字m8/自适应、WeTok 8PSK-FEC和感知Deep的RGB只读复用并重算指标，不混淆为同y控制。
- 首/末端正确native和noiseless单列诊断；失败不删除；连续均值不静默硬化；选中模型和相同新增更新机会逐行复核。
- CPU复核保存的AWGN、RGB hash/PSNR、连续特征MSE/诊断BER与时延次序。该核验不是新训练重复，也不是另一实现的图像重渲染。
- 15项CPU测试PASS，包括实际评测器的中断/续跑/重预热、共享观察张量、历史输出不改写、统计边界以及硬件查询只读性。
- `reference_preflight.json`：441条旧图最大PSNR差3.9263e-6 dB；21条父点信号最大平均复能量差4.7684e-7。新接收器GPU开发评测尚未执行。
- `plot_calibration.py`只读取已审计不可变里程碑，绘制完整校准和固定监控子集各自的曲线、训练损失/梯度/速度。监控子集不参与选模，不与完整校准当作相同人口直接比较。

## 本机运行条件

05:35实际查询：GPU0利用率约94%、显存5622 MiB；86–87°C时曾报告`SW Thermal Slowdown: Active`，随后查询也出现`Not Active`，即间歇节流。未更改功率上限、时钟、MPS、风扇或驱动，没有继续增加训练负载。

未来评测每帧循环轮换六方法时序。完整2100帧下，每方法每位置350次；每SNR每位置50次。所有方法仍使用同一收到的y，仅调整执行顺序，不是新方法变量。每源前后用只读`nvidia-smi`记录硬件状态；接收时间包括内部E重编码与最后视觉Decoder，不计离线算指标/存档，整体评测耗时另外记录。出现温控时，报告真实运行条件，不拿本轮时延与旧日志做无条件排名。

## 下一步

等首1000完整校准与watcher审计完成，先看完整曲线和创新量相对预测特征/state-history/full-grid以及有力single/no-history控制，再按共同预算推进2500/5000或记录有依据的停止。当前没有新机制系统胜出的结论，尚无新holdout或重复训练种子。
