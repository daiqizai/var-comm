# 2026-09-14中断恢复

10:15核查发现旧trainer 3205198、review watcher 3205199和finisher 3211225均不存在，GPU无计算进程。旧状态文件仍显示9月13日3000步监控，不能据此称一直在训练。系统本次启动时间为2026-09-14 10:10:44（UTC+8）；具体上次中断时刻/原因没有完全确认。

## 恢复依据

- `resume.pt`完整可读，SHA256 `cb56600b879275601247016d4c7e6fcf153372aefb0d974207947ba1c528c433`。
- 六个模型、每臂3000条训练记录及Adam状态均位于实际3000步，global data=10000；逐步重放核对数据/翻转/SNR/噪声指纹、实际共享y，全部通过。
- 六个E均保持原冻结hash，模型和optimizer张量有限；原选择checkpoint及冻结代码/profile/排队评测绑定均未变。
- 3000监控已完整保存在断点，恢复不会重复初始校准或从选中0/1000/2500点重新训练。

原状态、日志与启动记录已单独保留；断点以硬链接保存，不复制大型模型或移动旧路径。证据`outputs/WETOK-INNOVATION-R1-RECOVERY-20260914/audit.json`。

## 当前运行

10:22从实际3000恢复至原目标5000，trainer **25991**、review watcher **25992**、finisher **25993**。10:26已保存3100步。日志在`outputs/WETOK-INNOVATION-R1-RECOVERY-20260914/{trainer,review_watcher,finisher}.log`，实时状态仍为原实验的status/pipeline路径。

新的收尾队列继续执行原5000审计、27300主行＋600支持点评测、统计及绘图；不抢先开始Joint训练。没有修改模型、FEC、资源、loss、数据或噪声计划。

保存点以后的未落盘工作无法重建，不能把它说成0，也不能把一整天中断算成GPU训练时长。现有`observed_wall_GPU_hours`按已保存累计运行时间接续，是可核查的已观察时间；整段研究计算成本仍需同时注明未观测部分。总研究目标未完成。
