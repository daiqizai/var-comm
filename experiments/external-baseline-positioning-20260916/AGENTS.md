# External author-method positioning only

- Latest user scope: runtime diagnosis and a fixed exploratory20-source x five-SNR x original-seed2001 stage are explicitly allowed. Preserve the original1500-frame plan and240 committed frames; do not call the exploratory100-frame stage full completion.
- The original queue and full-closure waiter are paused. The finite stage20 pipeline uses original forced-attention-checkpoint execution; the isolated direct-attention calibration variant is not deployed. Do not modify the bound original scripts/vendor or launch duplicate jobs.
- Keep gradient/full-RGB/repeatability failures and measured costs. No full-path no_grad/inference_mode, new training, architecture or shared environment modification. Different record views are not distinct sampler invocations.

- User authorization: run published SwinJSCC and HiFi-DiffCom/its identical ADJSCC backbone; no new architecture, own-model training, CSI study, automatic parameter search, or new final holdout.
- Do not rerun the completed weight-closure, R3 completion, or existing full digital audits. Reuse matching results; supplement only missing SSIM, actual common-budget configurations and necessary timing.
- Use original 100 development images and fixed noises. Author-protocol qualification is separate from common-physics comparison. Freeze choices on calibration before new development results.
- Local historical projects, checkpoints and outputs are read-only. New code, isolated environments, upstream snapshots, assets and outputs stay in this experiment or VAR_COMM.
- Only local GPU0 is authorized, and only when not occupied by others. Do not kill other jobs or modify shared/global packages, drivers, clocks or MPS.
- Verify current upstream commit, asset source/SHA and state-dict coverage. Do not rename locally trained Swin variants as official pretrained models.
- SwinJSCC upstream warns about SA/RA inference under torch >1.12. Test an isolated compatible environment; do not treat compatibility errors as performance evidence.
- Charge actual complex uses, power and required side information. Do not mask a pretrained model to force N3060. Keep shared ADJSCC transmission/observation for the diffusion attribution comparison.
- Preserve engine failures and all scientific failures. DINO is report-only in this round. Do not select generation seeds using ground truth.
- Final deliverable: VAR_COMM/reports/external_baseline_positioning_result.md with actual per-image results, resource/cost ledgers and explicit unresolved reproduction boundaries.
