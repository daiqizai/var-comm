# 1600追加步的CPU梯度监测：不能把回退简单归因于权重

2026-09-12 19:00完成。使用同一个已保存的1600步检查点、固定四张训练图与噪声，三种continuous结构，共12个训练样本/模型组合。实际SNR覆盖7/13/19dB，不覆盖1/4；没有使用calibration或development，没有参数更新、没有占用GPU。

## 观察

- MSE与LPIPS梯度有2/12个组合为负余弦；全部参数的余弦范围约[-0.1848, +0.8016]。
- **实际注册权重组成的总梯度，与LPIPS梯度在12/12个组合中均为正余弦**，范围[+0.2877, +0.8177]。这不支持“总梯度普遍直接指向LPIPS变坏”的说法。
- MSE梯度范数与已乘0.01的LPIPS梯度范数之比约1.81–21.16，中位约3.91；不能只比较loss标量，也不能因范数较大就认定因果已查明。
- Encoder、Receiver分开统计得到同样的总梯度正向结论；所有冻结视觉参数和通信参数均未变化。

## 限制与决定

这是四张训练图的局部梯度，不是全分布分析、不是Adam真实更新方向，也不能解释所有校准回退。动量/预条件、有限步长、泛化和表示限制仍可能影响走势。不能据此认定现有权重最优，也不能直接决定将LPIPS或bits权重改大。

当前九臂仍按原配置至2500完整校准。需要修改目标或发送映射时另立受控比较；不把该监测扩成新增方法先过关的gate。

原始加权范数、逐参数分区余弦、来源hash与检查点SHA：`outputs/WETOK-CONTINUOUS-GRADIENT-PROBE-20260912-0001600/summary.json`。
