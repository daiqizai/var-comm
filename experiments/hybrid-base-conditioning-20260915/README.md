# 实际RX基图条件连续译码：唯一两臂实验

计划：`../../reports/hybrid_base_conditioning_protocol_20260915.md`。
配置：`../../configs/hybrid_base_conditioning.json`。
实时状态：`../../outputs/HYBRID-BASE-CONDITIONING-20260915/pipeline_001/status.json`及`training_001/status.json`。

只改变连续Decoder是否读取实际数字基图的空间特征。两个接收器均有同样的5488参数小模块与32/64中间层投影，总参数1648125；不扩原E/D骨干，不训练三个RGB gain。

同一模型内，y派生图与基图/零图两次提取共享f；**两臂模型与optimizer独立，不共享可更新存储**。控制的f权重仍由y支路学习，额外参数不是全部挂空。

两臂从旧add10000的E/D与Adam开始，初始函数用零投影严格匹配。旧点只作诊断初始化，不能据此改写旧失败结论。通信E/D和新空间模块都联合训练；因此两臂训练后的模拟波形可以不同，这不是“固定编码器/完全相同波形”的RX-only声称。额外的固定空间条件打乱诊断则保持条件模型自身的信号与最终相加基图不变。

沿用已审计的缓存、PHY和指标：68+1882+1110=3060，E6120，m7、实际失败保留、MSE+0.01LPIPS、20k训练/1k校准。每臂新增最少10000、最多20000；只要任一臂在完整校准继续改善，就同机会推进两臂。到上限仍改善须写预算截断，不写收敛。

机制区间7/13/19与系统区间1/4/7分别报告，均不筛正确帧、不以DINO选模、不访问新holdout。完整实际收发计时计入新特征提取，不用离线基图缓存减免在线成本。

按已冻结脚本有界运行：正式训练/校准→固定选择点→原100 development与单个条件打乱诊断→仅新两臂计时→统计报告。发生异常停止；不自动加模块、改资源或开新分支。文件/PID中的旧进度不作为重启依据。
