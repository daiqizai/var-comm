# Joint2500审计后继续5000

2026-09-14 14:32三臂2500更新及全校准/审计PASS，里程碑SHA `6e4ae8a551e314ab8e306c05b328bbd9fb3878567b8307a59da0b5485574c358`。

single/no-history/state选中LPIPS为0.197908/0.200233/0.198812；no-history仍选中0。single与state比各自同2500机会R-only略好，但Joint条件结构仍不及Joint single。详细结果见`reports/wetok_joint_milestone2500_20260914.md`，仅为校准结果。

## 固定继续项

- 三臂从实际2500模型和Adam恢复，各增加2500次更新至5000；不从所选0点重新开始。
- 共同原父点、结构、连续接口、视觉冻结、自身接收历史、原loss/学习率、micro1/effective4、配对数据/增强/SNR/标准噪声不变。
- 仍为0 header＋3060 data complex uses、总能量6120；无类别/caption/免费真实历史或反馈。
- global data由9500接至12000，保持与已完成R-only控制对应的训练机会；Joint各自波形可以不同，不声称同y。
- 完整校准选模仍只使用0/1000/2500/5000，LPIPS最小、同值取早；500步监控子集不参与选择，DINO不参与训练或选模。

继续理由是完整曲线尚在变化、实际训练与物理检查健康，完成既定5000机会能够与完整强控制比较；不是保证继续下降或宣布充分收敛。此时不改学习率/监督/规模来混淆E更新因素。

## 执行与收尾

14:33检测到其它GPU任务313578，先排队而不抢占。启动前再次核实2500模型/Adam与校准回执、源码绑定以及GPU空闲；若原状态改变则拒绝重复启动。不把排队称为正在训练。

实际命令为`train.py --until-update 5000 --resume --execute`及对应`watch.py --step 5000`。再以两者实际PID启动`finish_registered_trial.py`，等待5000及审计通过后执行原评测/分析，不启动其它科学分支。

33600主行、600 Deep支持点和900无噪诊断行保留原注册范围。最终要分别回答Joint相对R-only、条件结构相对single/no-history，以及相对full-grid/数字/Deep强系统的结果；不得只展示有利对照。若Joint条件结构获益，匹配Joint普通迭代控制仍是后续必要工作。

按目前真实速度，排队后的训练和完整校准粗估约75–100分钟，另有评测/CPU统计时间；硬件并发和温度会影响速度。这不是停止阈值，等待其它任务不计入训练GPU小时。研究总目标仍未完成。
