# Joint首1000结果与继续2500

2026-09-14 13:32完成三臂1000更新和完整校准，审计PASS。里程碑SHA：`780606cae4eedc0f09aa740b7bf747cbf508c4762a8522821db6b9ca157f0c98`。没有新development评测或test访问。

## 结果

完整1000校准图、五SNR、固定噪声。按LPIPS选择：

| Joint结构 | step1000 LPIPS | 选中step / LPIPS | 同1000机会R-only选中LPIPS |
|---|---:|---|---:|
| single | 0.201116 | 0 / 0.200233 | 0.198920 |
| no-history | 0.200713 | 0 / 0.200233 | 0.200233 |
| state-history | 0.201303 | 1000 / 0.201303 | 0.201941 |

不能用state从较差初始化下降的幅度制造结构增益，也不能把single/no-history选中0隐藏掉。当前Joint尚无明确整体收益：state对同结构R-only校准LPIPS仅−0.000637，single选择结果反而差+0.001314；这些是选模人口上的差值，不是独立确认。

## 继续规则

从三臂**实际1000末端模型/Adam**共同继续到2500，不从选中的0点重启；global data从8000接到9500。保持旧父点、视觉冻结、结构、loss、学习率、有效batch4/micro1、同数据/增强/SNR/噪声和N3060/E6120。

这是原5000候选日程的中间检查，并非保证多训会赢。E/R确实更新、数据/Adam与对照逐步对应，未发现数值或物理失效；不在此同时改变loss/温度/模型。2500全校准后再次判断，最终仍面对全部六个R-only和强系统。

启动profile只测前/反向，不含Adam/数据/校准，不能用其数值当完整工期。后续以真实记录估算，不改写旧profile或训练数据。
