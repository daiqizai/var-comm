# Joint5000评测的GPU竞争退出与断点恢复

2026-09-14 16:01，原evaluator443167在第50张源图的末端GPU检查中检测到其它进程455090，按原协议退出；原finisher348503随后记录失败并退出。不是训练/模型数值错误，也不是观察超时后推断任务消失。

## 保留的现场

- 5000训练、模型/Adam和全校准审计均已完成，不重启训练、不换checkpoint。
- 原100张development已提交前49张，共16464主行；`images/000`至`048`均有完整回执。
- `images/049`没有回执，目录为空。该源图计算遇到设备竞争，不能把它当通信失败丢出统计；恢复时用同一源图和固定噪声重算。
- 原评测metadata、第一次失败session、原finisher的NEEDS_REVIEW状态和日志全部保留，未改成成功。
- 第一次只读观察器357923于16:01正常退出，460次采样记录封存；没有据它的退出推断科学试验完成。

## 当前恢复方式

16:12启动**恢复监督器473779**及**新只读观察器473780**。前者只编排原`evaluate.py --step 5000 --resume --execute`和原`analyze.py`，不会运行train.py。当前是否正在计算或等待，以其进程和`outputs/WETOK-JOINT-SENDER-R1-EVAL-RECOVERY-20260914-001/status.json`为准。

新脚本为`scripts/resume_joint_evaluation.py`，未修改任何已绑定的模型、原评测、统计或原finisher源码。它已重新核验5000模型/校准/强控制、原评测metadata和前49张源图回执SHA；资格记录在上述恢复目录的`qualification.json`。

- GPU连续30秒检查未见其它计算任务后，才启动同协议断点回放；不结束其它任务，不改MPS、驱动、时钟或功率。
- 原评测仍在每张源图计算前后检查GPU占用，不绕过保护。已提交的源图由原实现校验并复用，没有新的噪声挑选或样本过滤。
- 若再次因同类GPU竞争退出，保留该次日志/状态，核实实际子进程末尾确为对应RuntimeError后再等待恢复。仅旧状态文件残留、SIGKILL、数值/源码/数据错误等均不触发自动重试。
- 100张全部完成后自动跑原统计与CPU复核；不手动开启第二份评测或分析。
- 排队等待单列，不计为训练时间；每次实际评测进程和原session都保留。状态中的已结算等待时间在每段等待结束时更新。

新增4项恢复工具测试，连同原测试共31项CPU测试通过。它们检查失败类型、真实子进程日志、旧回执不变及dry-run边界，不是额外模型质量证据。

16:17第一次恢复实际启动evaluator480961，16:17:58已提交第50张。16:19在55张提交后再次检测到GPU进程482008，原保护再次退出；恢复器通过真实子进程日志确认该原因，保留该次记录并自动返回等待，未绕过检查。原49张回执及冻结源码已再次核验不变。

当前仍没有完整Joint development结论，不根据前55张的部分结果选择后续方案。
