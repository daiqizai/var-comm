# Short-prefix study
The user authorized execution of EXPERIMENT_PLAN.md on 2026-09-23. This adds new
communication arms and a future unseen communication-test population as explicit
exceptions to older experiment restrictions; it does not reopen original A/B.
Use the sole root Git workflow. GPU0 only, yield to other GPU work, checkpoint
and pause on sustained thermal throttling. Never edit active source dependencies.
All modes share one training engine and one TX/channel/RX implementation. Keep
legacy 7/8/9 protocol functions unchanged; short-prefix protocol is versioned.
Complete CPU then real-weight qualification before bulk training. Main matrix,
calibration-only selection, budget confirmation, training-seed repeats and final
unseen test are separate stages. Never mark pending stages complete.
