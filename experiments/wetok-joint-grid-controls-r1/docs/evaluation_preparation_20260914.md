# Grid评测准备与自动收尾

2026-09-14新增评测链路已经通过18项CPU测试，并单独核验原100张输入像素的dtype/形状/内容哈希、33600条冻结参考及源文件绑定。它没有改变正在运行的训练、原模型或旧结果。

## 质量阶段

- 原100图×七SNR×三噪声，两个新模型只新增4200行；16个原方法逐行冻结复用，合计18方法/37800主行。
- 新200条与旧900条无噪诊断、600条固定支持Deep记录全部保留。旧质量/选择/物理字段及图像指针必须原值不变，旧计时只转入archived字段，当前质量表时间为空。
- 新源图先核对原始浮点源图哈希和原生token，实际保存s/y、恢复特征和图像。最后CPU复核全部新旧图像指针、PSNR算术及新模型的AWGN/能量/特征指标。
- 按用户关于共享GPU的方向，收尾使用显式`--shared-quality`，质量子进程带`--shared-gpu`；开始至少8 GiB空余，PyTorch分配器上限8 GiB，每图前至少2 GiB空余。不会因仅出现其它GPU进程就丢弃质量结果，但空间不足时保留现场并恢复。不是全部CUDA内存或算力的硬隔离，不保证双方不变慢，不改MPS/驱动/时钟。

## 独立计时

- 预先固定32个源图、五SNR、噪声2001，11个学习接收器共同计时，共1760行。
- 直接回放质量阶段保存的相同y，核对发送信号和重建图像；既有no-history仍去掉不影响图像的辅助读取。
- 计时要求GPU空闲，发生其它任务竞争时只重试未提交计时源图，不重做已完成质量，也不把共享时间放入速度排名。

## 验证与边界

18项CPU测试覆盖模型/微批次梯度、真实训练断点、完整质量矩阵/配对统计/物理账本、参考复用、共享准入、真实质量与计时driver的中断恢复、冻结质量不变、测量顺序和资源错误分类。小型fixture是工程测试，不是新科学结果；未训练模型未被要求先在质量上胜出。

原100图仍是development。接收时延的32图表不与旧100图全噪声时间混作同一个样本集；普通迭代不称为next-scale。新的质量、统计、计时文件及来源均有独立回执。

## 实际队列

finisher **689944**、完成期观察器 **689945**于19:03启动，等待当前trainer650391和reviewer650392完成5000及审计，然后依次运行`evaluate_quality.py`、`analyze_quality.py`、`evaluate_timing.py`。不要重复手动执行这些阶段。

状态为`outputs/WETOK-JOINT-GRID-CONTROLS-R1-step0005000-finish.pipeline.json`，各阶段PID和日志路径写入其中。评测/收尾源码、配置与协议均已绑定SHA，不边跑边修改。有真实缺陷必须保留原证据并另立修订，不改变哈希放行。
