# 完整Joint结果后的网格控制决定

2026-09-14原Joint5000已完成33600主行、全部强控制及CPU审计，分析回执SHA为`e2ba7b9a03d143d088d691c3c4db1aebc8ec49a22f270a8bdb3861d96563dc2f`。这不是依据未完成的前55张作决定。

Joint state相对Joint single的主区间LPIPS差为−0.002615，95% CI[−0.003608, −0.001680]；相对no-history也改善。但与R-only full-grid差−0.000295、CI跨0，并在PSNR/DINO上落后，且仍有无噪声映射损失。

因此落实此前`protocol.md`的两个控制，不再先加损失/功率或更换视觉模型。普通三次16/16/16读取是尺度归因所需控制；Joint full-grid innovation是同发送学习政策下的强迭代参照。两者仍用原7000父点、相同数据/噪声与新5000机会，旧三Joint不重训。

现有5项CPU模型/梯度检查及真实父点初始化通过，但尚无真实GPU训练。允许下一步在已授权GPU0上做零更新真实图像梯度/profile；通过后先训练到1000并完整校准审计，再共同推进2500/5000。此文件是执行决定，不等于GPU profile或训练已经完成。

保持现有源文件/模型/结果哈希不变。普通迭代不称为next-scale；这些控制即使胜出也不自动证明新颖性或总研究目标完成。GPU共享不等于设备设置变更：不改驱动/MPS/时钟，不停其它任务；当前旧评测已经完整结束，不为图方便改写旧独占保护。
